import 'package:flutter/material.dart';

class ExpensesWebPage extends StatelessWidget {
  const ExpensesWebPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('web/expenses'),
      ),
    );
  }
}
